/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import { useState, useEffect } from "react";
import { Shield, Activity, AlertTriangle, CheckCircle, Sun, Moon } from "lucide-react";
import { motion, AnimatePresence } from "motion/react";

export default function App() {
  const [isDark, setIsDark] = useState(() => {
    try {
      const stored = localStorage.getItem("sp-theme");
      if (stored !== null) return stored === "dark";
      return window.matchMedia("(prefers-color-scheme: dark)").matches;
    } catch {
      return false;
    }
  });

  useEffect(() => {
    localStorage.setItem("sp-theme", isDark ? "dark" : "light");
  }, [isDark]);

  return (
    <div className={`${isDark ? "dark" : ""} min-h-screen bg-slate-50 dark:bg-[#080d17] text-slate-900 dark:text-slate-100 font-sans`}>
      {/* Top Navigation */}
      <header className="bg-white dark:bg-[#0e1829] border-b border-slate-200 dark:border-[#1e2d45] sticky top-0 z-10">
        <div className="max-w-5xl mx-auto px-4 h-16 flex items-center justify-between">
          <div className="flex items-center gap-2">
            <div className="shield-logo bg-indigo-600 p-1.5 rounded-lg">
              <Shield className="w-5 h-5 text-white" />
            </div>
            <h1 className="text-xl font-bold tracking-tight text-slate-900 dark:text-slate-100">
              SecurePulse
            </h1>
          </div>
          <nav className="flex items-center gap-4 text-sm font-medium text-slate-600 dark:text-slate-400">
            <a href="#" className="text-indigo-600 dark:text-indigo-400">Daily Brief</a>
            <a href="#" className="hover:text-slate-900 dark:hover:text-slate-100 transition-colors">Sources</a>
            <a href="#" className="hover:text-slate-900 dark:hover:text-slate-100 transition-colors">Settings</a>

            {/* Animated dark mode toggle */}
            <button
              onClick={() => setIsDark(!isDark)}
              className="relative w-9 h-9 flex items-center justify-center rounded-lg text-slate-500 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-[#1e2d45] transition-colors overflow-hidden"
              aria-label="Toggle dark mode"
            >
              <AnimatePresence mode="wait" initial={false}>
                {isDark ? (
                  <motion.span
                    key="sun"
                    initial={{ opacity: 0, rotate: -90, scale: 0.8 }}
                    animate={{ opacity: 1, rotate: 0, scale: 1 }}
                    exit={{ opacity: 0, rotate: 90, scale: 0.8 }}
                    transition={{ duration: 0.2 }}
                    className="absolute"
                  >
                    <Sun className="w-4 h-4" />
                  </motion.span>
                ) : (
                  <motion.span
                    key="moon"
                    initial={{ opacity: 0, rotate: 90, scale: 0.8 }}
                    animate={{ opacity: 1, rotate: 0, scale: 1 }}
                    exit={{ opacity: 0, rotate: -90, scale: 0.8 }}
                    transition={{ duration: 0.2 }}
                    className="absolute"
                  >
                    <Moon className="w-4 h-4" />
                  </motion.span>
                )}
              </AnimatePresence>
            </button>
          </nav>
        </div>
      </header>

      {/* Main Content */}
      <main className="max-w-5xl mx-auto px-4 py-8">
        <div className="mb-8">
          <h2 className="text-2xl font-bold tracking-tight mb-1">Today's Intelligence Brief</h2>
          <p className="text-slate-500 dark:text-slate-500">Generated by Gemini 3 Flash • {new Date().toLocaleDateString()}</p>
        </div>

        {/* Placeholder for Brief Cards */}
        <div className="space-y-6">

          {/* Example Card 1 — CRITICAL */}
          <article className="bg-white dark:bg-[#0e1829] rounded-2xl border border-slate-200 dark:border-[#1e2d45] shadow-sm overflow-hidden">
            <div className="h-1.5 w-full bg-red-500"></div>
            <div className="p-6">
              <div className="flex items-start justify-between mb-4">
                <div className="flex items-center gap-2">
                  <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-semibold bg-red-50 dark:bg-red-950/40 text-red-700 dark:text-red-400 border border-red-100 dark:border-red-900/50">
                    <AlertTriangle className="w-3.5 h-3.5" />
                    CRITICAL
                  </span>
                  <span className="text-sm text-slate-500 dark:text-slate-500 font-medium">CISA KEV Addition</span>
                </div>
                <span className="text-xs font-mono text-slate-400 dark:text-slate-600">HIGH CONFIDENCE</span>
              </div>

              <h3 className="text-xl font-bold mb-3">Active Exploitation of Ivanti Connect Secure (CVE-2024-21893)</h3>

              <ul className="space-y-2 mb-6">
                <li className="flex items-start gap-2 text-slate-700 dark:text-slate-300">
                  <div className="mt-1.5 w-1.5 h-1.5 rounded-full bg-slate-400 dark:bg-slate-600 shrink-0"></div>
                  <p>A server-side request forgery (SSRF) vulnerability in the SAML component allows attackers to bypass authentication.</p>
                </li>
                <li className="flex items-start gap-2 text-slate-700 dark:text-slate-300">
                  <div className="mt-1.5 w-1.5 h-1.5 rounded-full bg-slate-400 dark:bg-slate-600 shrink-0"></div>
                  <p>Observed in targeted campaigns against aerospace and defense sectors.</p>
                </li>
              </ul>

              <div className="bg-slate-50 dark:bg-[#0a1220] rounded-xl p-4 mb-4 border border-slate-100 dark:border-[#1a2840]">
                <h4 className="text-sm font-bold text-slate-900 dark:text-slate-100 mb-1">Why it matters to you</h4>
                <p className="text-sm text-slate-600 dark:text-slate-400">You are subscribed to alerts for "Ivanti". This flaw allows unauthenticated access to internal resources and is currently being exploited in the wild.</p>
              </div>

              <div className="flex items-center justify-between pt-4 border-t border-slate-100 dark:border-[#1e2d45]">
                <div className="flex items-center gap-2 text-sm font-medium text-emerald-700 dark:text-emerald-400">
                  <CheckCircle className="w-4 h-4" />
                  Suggested Action: Apply patch version 22.4R2.2 immediately.
                </div>
              </div>
            </div>
          </article>

          {/* Example Card 2 — HIGH */}
          <article className="bg-white dark:bg-[#0e1829] rounded-2xl border border-slate-200 dark:border-[#1e2d45] shadow-sm overflow-hidden">
            <div className="h-1.5 w-full bg-amber-500"></div>
            <div className="p-6">
              <div className="flex items-start justify-between mb-4">
                <div className="flex items-center gap-2">
                  <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-semibold bg-amber-50 dark:bg-amber-950/40 text-amber-700 dark:text-amber-400 border border-amber-100 dark:border-amber-900/50">
                    <Activity className="w-3.5 h-3.5" />
                    HIGH
                  </span>
                  <span className="text-sm text-slate-500 dark:text-slate-500 font-medium">Threat Campaign</span>
                </div>
                <span className="text-xs font-mono text-slate-400 dark:text-slate-600">MEDIUM CONFIDENCE</span>
              </div>

              <h3 className="text-xl font-bold mb-3">Scattered Spider Shifts Tactics to Cloud Identity Providers</h3>

              <ul className="space-y-2 mb-6">
                <li className="flex items-start gap-2 text-slate-700 dark:text-slate-300">
                  <div className="mt-1.5 w-1.5 h-1.5 rounded-full bg-slate-400 dark:bg-slate-600 shrink-0"></div>
                  <p>The threat group is increasingly targeting Okta and Entra ID administrators via sophisticated social engineering.</p>
                </li>
                <li className="flex items-start gap-2 text-slate-700 dark:text-slate-300">
                  <div className="mt-1.5 w-1.5 h-1.5 rounded-full bg-slate-400 dark:bg-slate-600 shrink-0"></div>
                  <p>Bypassing MFA using prompt bombing and SIM swapping techniques.</p>
                </li>
              </ul>

              <div className="bg-slate-50 dark:bg-[#0a1220] rounded-xl p-4 mb-4 border border-slate-100 dark:border-[#1a2840]">
                <h4 className="text-sm font-bold text-slate-900 dark:text-slate-100 mb-1">Why it matters to you</h4>
                <p className="text-sm text-slate-600 dark:text-slate-400">Your organization uses Okta (detected via subscriptions). This represents a direct threat to your identity perimeter.</p>
              </div>

              <div className="flex items-center justify-between pt-4 border-t border-slate-100 dark:border-[#1e2d45]">
                <div className="flex items-center gap-2 text-sm font-medium text-indigo-700 dark:text-indigo-400">
                  <Shield className="w-4 h-4" />
                  Suggested Action: Enforce FIDO2/WebAuthn hardware keys for all admin accounts.
                </div>
              </div>
            </div>
          </article>

        </div>
      </main>
    </div>
  );
}
